[P#H1#流程]
改进型时钟算法用（访问位，修改位）表示页面状态，优先淘汰[T#1#未修改的页面]避免 I/O；四轮扫描依次找：① [T#B#(0,0)]（不修改标志位）→ ② [T#B#(0,1)]（本轮把扫描过的帧访问位设为 0）→ ③ (0,0)【原 (1,0)】（不修改标志位）→ ④ (0,1)【原 (1,1)】。
---
解析：其他条件相同时，应优先淘汰没修改过的页面——换出修改过的页面需要写回外存。
