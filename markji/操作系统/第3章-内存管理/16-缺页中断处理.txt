[P#H1#流程]
缺页中断处理（※※）：访问页面状态位为 0 时产生缺页中断 → 缺页进程[T#1#阻塞]放入阻塞队列 → 有空闲块则装入并修改[T#2#页表 Page 和快表 TLB]的页表项 → [T#B#没有空闲块]则由页面置换算法淘汰页面（修改过的要写回外存）→ 调页完成后唤醒进程放回[T#B#就绪队列]。
---
解析：[T#!!fff59d#缺页中断不是中断，是异常]（内部异常），在[T#B#指令执行期间]而非一条指令执行完后产生和处理。
缺页中断会同时把页面换到[T#B#快表 TLB 和慢表 Page]中。
