[P#H1#数字]
请求分页的页表项在基本分页基础上增加的字段：[F#1#状态位 P]（是否已调入内存）、[F#2#访问字段 A]（供置换算法参考）、[F#3#修改位 M]（是否被修改过）、[F#4#外存地址]（供调入参考）。
---
解析：请求分页 = 基本分页 + [T#B#请求调页功能] + [T#B#页面置换功能]。
修改位为 0 的页面换出时[T#B#不用写回外存]；修改过才需写回覆盖外存旧数据。
