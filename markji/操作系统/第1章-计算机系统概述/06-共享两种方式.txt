[P#H1#对比]
共享分两类：[F#1#互斥共享]——一个时间段内只允许一个进程访问该资源；[F#2#同时共享]——一段时间内允许多个进程访问（微观上"分时共享"交替进行）。
---
解析：[T#B#临界资源（独占资源）]——在一段时间内只允许一个进程访问的资源；大多数物理设备及某些软件中的栈、变量和表格都属于临界资源，要求互斥共享（如摄像头同一时段只能分给一个进程）。
同时共享例：QQ 与微信同时向磁盘发送文件，宏观同时、微观交替。
