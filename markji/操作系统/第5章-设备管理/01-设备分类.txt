[P#H1#对比]
按信息交换单位分类：[T#1#块设备]（以[T#B#数据块]为单位、有结构、传输速率高、可寻址，如硬盘磁盘，I/O 常采用[T#B#DMA]）与[T#2#字符设备]（以[T#B#字符]为单位、无结构、速率慢、[T#B#不可寻址]，输入/输出常采用[T#B#中断驱动]方式，如键盘鼠标）。
---
解析：按传输速率——低速（鼠标键盘）、中速（打印机）、高速（硬盘、光盘机）；按使用特性——人机交互类（慢）、存储设备（快）、网络通信设备（介于二者之间）。
每台设备确定一个编号以区分识别，称为设备的[T#B#绝对号]。
