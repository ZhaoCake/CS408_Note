[P#H1#对比]
I/O 接口三类：[T#1#字符设备接口]（传输速率低、不可寻址、中断驱动、[T#B#字符设备都是独占设备]，需提供打开/关闭操作实现互斥共享）、[T#2#块设备接口]（[T#B#磁盘是典型块设备]，可寻址、常采用[T#B#DMA]方式，隐藏磁盘二维结构为线性编号，支持内存映射接口）、[T#3#网络设备接口]（套接字接口）。
---
解析：I/O 模式——阻塞 I/O（操作完成才返回，[T#B#大多数 OS 采用]）；非阻塞 I/O（立即返回，数据未就绪则返回错误或特殊值，定期检查）；异步 I/O（发起后继续执行其他任务，完成时系统通知并提供数据）。
