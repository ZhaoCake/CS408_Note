[P#H1#易错]
父进程创建子进程后，父进程与子进程[T#B#同时执行（并发）]；[T#!!fff59d#父进程和子进程可以共享一部分资源，但不能共享虚拟地址空间]。
---
解析：子进程可以继承父进程所拥有的资源；子进程被撤销时应将获得的资源归还父进程；撤销父进程时必须[T#B#同时撤销其所有子进程]。
对比——主程序调用子程序后，主程序[T#B#暂停]在调用点，子程序执行完返回后主程序才开始执行。
不能共享虚拟地址空间的原因：虚拟地址是逻辑地址，且父子进程有不同的 PCB 用于区分进程。
