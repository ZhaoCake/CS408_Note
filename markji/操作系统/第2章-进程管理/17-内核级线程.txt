[P#H1#概念]
内核级线程 KLT 的管理工作均由[T#1#操作系统内核]完成，切换必然需要在[T#2#核心态]下完成；[T#!!fff59d#操作系统只能看见内核级线程，所以只有内核级线程才是处理机分配的单位]。
---
解析：优点——能发挥多处理机优势（内核可同时调度同进程多线程并行）；一个线程被阻塞可调度同进程其他线程；线程切换较快开销小。
缺点——[T#B#同一进程中的线程切换需要从用户态转到核心态]，系统开销较大。
