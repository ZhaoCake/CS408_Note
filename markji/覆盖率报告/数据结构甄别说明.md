# 数据结构 8 章未覆盖条目甄别结论

> 背景：数据结构 8 章制卡完成后统一运行覆盖率审计（`scripts/coverage_audit.py`），对全部未覆盖条目逐条甄别。
> 日期：2026-08-31

## 核心结论

DS 共 **48 条未覆盖**（第2章 0、第3章 1、第4章 7、第5章 10、第6章 9、第7章 12、第8章 8），逐条甄别后：

- **真漏卡 0 条**
- 全部为以下四类可解释项：公式/推导细节、算法操作过程叙述、HTML/代码残留噪声、词形差异误报

## 分类说明（代表性例子）

| 类别 | 说明 | 代表 |
|---|---|---|
| 公式推导细节 | EX 中 next/B 树/红黑树证明过程，结论已抽卡 | DS4 的 'p1p2...' 前缀公式、DS5 红黑树"最长路径≤2倍"证明 |
| 操作过程叙述 | 算法步骤的逐字叙述，思想已入卡 | DS5 非递归中序遍历步骤、哈夫曼构造重复句 |
| 残留噪声 | 红黑树 `<span style>` HTML 颜色标记、C++ 代码词 | DS7 的 span/background/red/black、DS4 的 std/string/cout |
| 词形差异 | 卡片用概括表述，EX 用展开句 | "让子树接替" vs "子树成为父结点的子树"、"染红" vs "染为红色" |

## 反向溯源（通道③）核查

报出的无依据挖空词全部为**表述差异**（卡片概括 vs 笔记展开），非真实无依据，例如：
- DS7 `08-BST删除` 的"让子/接替"（笔记写"子树称为父结点的子树"）
- DS7 `11-B+树` 的"仅含/必到/相连"
- DS5 `20-B树性质` 的"因子/点子"（"平衡因子"分词差异）

## 词频盲区（通道④）

盲区词全部为通用词或代码/HTML 残留（span、std、string、root 等），无真实考点盲区。

## 结论

数据结构制卡质量与前序课程一致；DS 第 1 章无 EX 文件（不在考纲），覆盖率显示 N/A 属正常。
