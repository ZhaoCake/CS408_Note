# 数据结构 第7章-查找 覆盖率报告

- 卡片数：16
- EX 考点条目：171
- 已覆盖 83 / 部分 58 / 跨章 18 / 未覆盖 12
- **完全覆盖：48.5%；含部分+跨章：93.0%**

## ① EX 考点：未覆盖 / 跨章
| 条目 | 状态 | 未命中词 |
|---|---|---|
| 8 查找算法的分析及应用 | 跨章 |  |
| 1 顺序查找（线性查找）：常用于线性表，从头到尾逐个查找。 | 跨章 | 从头到尾、逐个 |
| 1 变幻公式：(low+high)2low2+high2或low-(low2-high2)low+(high-low)2。 | 未覆盖 | low2、2low2、high2、变幻 |
| 1 若树非空，目标值与根结点的值比较。 | 未覆盖 | 若树、目标值 |
| 3 而B的原右子树则作为A结点的左子树。gflchild/rchild=p（让结点A的原双亲gf的左/右孩子指向B）。 | 未覆盖 | 树则、原右子、gflchild |
| 3 而B的原左子树则作为A​结点的右子树。gf- lchild/rchild=p（让结点A的原双亲gf的左/右孩子指向B）。 | 跨章 | 树则 |
| 2 根结点是黑色的。 | 跨章 |  |
| 3 叶结点都是黑色的，保证红黑树的内部结点左右孩子均非空。叶结点为NULL结点，和n+1个虚构的外部结点 | 跨章 | 虚构 |
| 5 对每个结点，从该结点到任一叶结点的简单路径上，所含黑结点的数量相同。 | 未覆盖 | 所含、到任、一叶 |
| 1 从根到叶结点的最长路径不大于最短路径的两倍。 | 跨章 | 两倍 |
| 2 有n个内部结点的红黑树的高度h22(n+1)。 | 跨章 | h22 |
| 1 新结点是根一一染为<span style="background-color: black; color: white; | 未覆盖 | span、一一、black、style、background、white、染为、color |
| 2 新结点非根一一染为<span style="background-color: red; color: white;"  | 未覆盖 | span、red、一一、style、background、white、染为、color |
| 2 若插入新结点后依然满足红黑树定义，即父结点是<span style="background-color: black;  | 未覆盖 | span、black、style、background、即父、white、color |
| 1 兄弟为红色：将兄弟变成黑色，父结点变成红色；对父结点左旋，恢复左子树的黑色高度，左侄子成为新的兄弟。 | 未覆盖 | 对父、恢复、变成、成为、侄子 |
| 3 在所有的B树结点中，黑色结点是父结点，红色结点是子结点。黑色结点在中间，红色结点在两边。 | 未覆盖 | 中间、两边、是子 |
| 1 根结点的子树数2,m ，关键字数1,m-1 。 | 跨章 |  |
| 2 其他结点的子树数{m}{2},m ，关键字数{m}{2}-1,m-1 。 | 跨章 |  |
| 7 最小高度：h(n+1)。 | 跨章 |  |
| 8 最大高度：h{{m}{2}}{n+1}{2}+1。 | 跨章 |  |
| 9 对于高度为h的m阶B树最多有(m-1)(1+m+m^2++m^{h-1})个结点。 | 跨章 |  |
| 10 对于高度为h的m阶B树最少有1+2({m}{2}^{h-1}-1)个结点。 | 跨章 |  |
| 2 若父结点插入时也溢出了，则同理在父结点的中间进行分割，左半部分在原来父结点，右半部分新建一个父结点，并把中间结点右边开始 | 未覆盖 | 原父、连接、右半、新父、左半、新建、上移、中间、原来 |
| 1 若被删除关键字在终端结点，且结点关键字个数不低于下限，则直接删除该关键字，并移动后面的关键字。 | 跨章 | 低于、下限 |
| 3 原结点右兄弟结点里的关键字全部前移一位。 | 跨章 | 前移 |
| 3 原结点左兄弟结点里的关键字全部前移一位。 | 跨章 | 前移 |
| 3 左右兄弟都不够借：若左右兄弟结点的关键字个数均等于下限值，则将关键字删除后与左或右兄弟结点以及父结点中的关键字进行合并： | 跨章 | 不够、限值 |
| 2 将原结点的左或右兄弟结点的关键字合并到原结点（前插或后插），并将连接也转移到原结点上。 | 跨章 | 转移、连接 |
| 3 若父结点的关键字个数又不满于下限，则父结点同样要于与它的兄弟父结点进行合并，并不断重复这个过程。 | 未覆盖 | 同样、要于、下限、不满 |
| 4 若父结点为空则删除父结点。（兄弟合并，父亲下沉） | 未覆盖 | 父亲、空则、下沉 |

## ② 笔记补漏未命中（<30% 关键词命中）
| 笔记考点 | 未命中词 |
|---|---|
| 本章是考研命题的重点 | 命题、本章 |
| 值得注意的是，分块查找适用于某些特定场景，当块的大小$ m $和索引表的大小$ n $合适时，分块查 | 数据量、程度、算法、较大、场景、值得注意、性能、当块、适合、受到 |
| 内部结点数最少的情况为总共$h$层黑结点的满树形态 | 满树、内部、形态 |
| 处理：把插入结点作为根结点，并把结点设置为<span style="background-color | span、黑色、black、style、background、white、color |
| 不会发生冲突 | 发生冲突 |

## ③ 反向溯源：挖空词在笔记中找不到依据
全部卡片挖空词均能在笔记中找到依据。

## ④ 词频盲区：笔记高频但卡片未出现
| 高频词 | 笔记次数 |
|---|---|
| span | 36 |
| color | 32 |
| style | 19 |
| 中间 | 16 |
| video | 15 |
| 查询 | 14 |
| background | 14 |
| white | 14 |
| 磁盘 | 11 |
| 变成 | 10 |
| red | 10 |
| src | 9 |
| black | 9 |
| 树上 | 8 |
| mp4 | 8 |
| 侄子 | 8 |
| 连接 | 8 |
| 下面 | 7 |
| 这种 | 7 |
| 合适 | 7 |
