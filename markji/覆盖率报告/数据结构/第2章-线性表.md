# 数据结构 第2章-线性表 覆盖率报告

- 卡片数：11
- EX 考点条目：6
- 已覆盖 5 / 部分 0 / 跨章 1 / 未覆盖 0
- **完全覆盖：83.3%；含部分+跨章：100.0%**

## ① EX 考点：未覆盖 / 跨章
| 条目 | 状态 | 未命中词 |
|---|---|---|
| 1 线性表的基本概念 | 跨章 |  |

## ② 笔记补漏未命中（<30% 关键词命中）
| 笔记考点 | 未命中词 |
|---|---|
| `new` 关键字 | new、关键字 |
| malloc() 函数 | malloc、函数 |
| 需要将返回的指针强制类型转换为适当的指针类型 | 强制、返回、适当、类型转换 |
| +  注意，在C++中使用 `malloc()` 时，**需要将返回的指针强制类型转换为适当的指针类 | 强制、c++、返回、适当、malloc、类型转换 |
| +  注意，与 `malloc()` 相对应，需要确保释放的是通过 `malloc()` 分配的内存 | 确保、未定义、行为、malloc、释放 |
| - 注意事项： | 注意事项 |
| new 和 malloc 的区别 | malloc、new |
| other.x) && (y | other |
| obj2.x) && (obj1.y | obj2、obj1 |
| `memset`函数 | memset、函数 |
| 需要注意的是， `memset` 通常用于处理原始的内存块，对于 C++ 对象或包含指针的结构体等复 | 运算符、推荐、正确、c++、体等、赋值、构造函数、行为、初始化、数据结构 |

## ③ 反向溯源：挖空词在笔记中找不到依据
| 卡片 | 未在笔记出现的挖空词 |
|---|---|
| 08-指定结点前插O1技巧.txt | 放入、搬到 |

## ④ 词频盲区：笔记高频但卡片未出现
| 高频词 | 笔记次数 |
|---|---|
| int | 30 |
| new | 26 |
| current | 21 |
| ptr | 20 |
| malloc | 19 |
| listnode | 18 |
| prev | 16 |
| head | 14 |
| nullptr | 13 |
| c++ | 12 |
| 节点 | 12 |
| 分配内存 | 11 |
| memset | 11 |
| cpp | 10 |
| 代码 | 9 |
| arr | 9 |
| std | 9 |
| 表头 | 8 |
| 参数 | 8 |
| delete | 8 |
