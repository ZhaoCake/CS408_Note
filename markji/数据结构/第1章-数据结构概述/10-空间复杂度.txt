[P#H1#判据]
空间复杂度——衡量算法使用内存空间随数据量增大的增长趋势；统计范围为[T#1#暂存空间 + 输出空间]（不含输入空间，指令空间通常忽略）；[T#!!fff59d#只关注最差空间复杂度]，含"最差输入数据"与"最差时间点"两层含义。
---
解析：暂存空间 = 暂存数据 + 栈帧空间 + 指令空间。递归函数同时存在 n 个未返回的栈帧时占 O(n) 空间；循环中反复调用函数（每轮返回并释放）仍为 O(1)。
