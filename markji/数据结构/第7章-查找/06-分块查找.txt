[P#H1#公式]
分块查找（索引顺序查找）：[T#1#块内无序、块间有序]；[T#2#可以采用链表]；是[T#3#静态查找]；n 个元素分 b 块每块 s 个，索引顺序查找时 [T#4#ASL=(b+1)/2+(s+1)/2]，当 [T#5#s=√n 时 ASL 最小=√n+1]；索引折半查找时 [T#6#ASL=⌈log₂(b+1)⌉+(s+1)/2]。
---
解析：先查索引表定位分块（可顺序可折半），再到分块内顺序查找；折半查找索引表失败时停在 low 所指向的分块。
