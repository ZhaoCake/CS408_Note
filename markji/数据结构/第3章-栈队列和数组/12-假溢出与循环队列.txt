[P#H1#概念]
顺序队列的假溢出——[F#1#出队使前面空间空闲，但队尾指针不断 +1 直到指向最后区域，计算机误认为已满，实际未满]；[T#B#用循环队列解决]：指针加一后取模 MaxSize。
---
解析：循环队列初始 Q.front=Q.rear=0；进队 Q.rear=(rear+1)%MaxSize；出队 Q.front=(front+1)%MaxSize；队列长度 = (rear+MaxSize−front)%MaxSize。
