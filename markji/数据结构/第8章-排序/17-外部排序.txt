[P#H1#判据]
外部排序：[T#1#数据量大无法全部读入内存，需多次内外存交换，最大时间开销是读写外存时间]；[T#2#通常用归并排序，最少只需三个缓冲区（两个输入+一个输出）]；k 路归并归并趟数 [T#3#⌈logₖr⌉]；优化方向——[T#4#增大归并路数、减少归并段个数]。
---
解析：k 增大需 k 个输入缓冲区（内存开销增）、每选一关键字比较 k−1 次（可用败者树优化）；外部排序时间=读写外存+内部排序+内部归并。
