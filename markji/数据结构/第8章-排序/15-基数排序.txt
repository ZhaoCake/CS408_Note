[P#H1#判据]
基数排序：[T#1#不是基于比较的排序，比较次数为 0]；只能对整数排序；[T#2#需要 r 个辅助队列，空间 O(r)]；时间复杂度 [T#3#O(d(n+r))]（d 趟分配收集）；稳定；[T#4#移动次数与关键字的初始排列无关]。
---
解析：按关键字位权重递增次序（个十百）做 d 趟分配+收集；擅长解决的问题——关键字可拆分为 d 组且 d 较小、每组取值范围小（r 小）、元素个数 n 较大。
