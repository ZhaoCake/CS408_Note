[P#H1#判据]
Dijkstra 算法：[T#1#贪心策略]（优先选 dist 最小的顶点加入）；用 final/dist/path 三个数组；时间复杂度 [T#2#O(|V|²)]（邻接矩阵和带权邻接表相同）；[T#!!e53935#不适用于含有负权值的带权图]。
---
解析：每次从 final=false 且 dist 最小的顶点出发，更新其邻接顶点的 dist 和 path；重复 n−1 轮。
