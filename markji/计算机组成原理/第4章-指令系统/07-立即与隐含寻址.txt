[P#H1#对比]
立即寻址：形式地址指出的[T#1#不是地址而是数据本身]（立即数，[T#!!fff59d#采用补码形式存储]，用 `#` 表示立即寻址特征），共访存 [T#2#1] 次；隐含寻址——累加器 ACC 对单地址指令是隐含寻址（操作数地址隐含）。
---
解析：立即寻址采用[T#B#定长指令码]格式时指令执行时间最短；缺点——A 的位数限制立即数范围（A 为 n 位补码时范围 [−2^(n−1), 2^(n−1)−1]）。
隐含寻址优点——缩短指令字长；缺点——需增加存储隐含地址的硬件。
