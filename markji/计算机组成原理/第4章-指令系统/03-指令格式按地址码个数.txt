[P#H1#概念]
按地址码个数分类：零地址指令（[T#1#只有操作码，如空操作/停机/关中断]，运算类仅用于堆栈计算机）；一地址（单操作数 [T#B#OP(A1)→A1] 访存 3 次；隐含 ACC 的双操作数 [T#B#(ACC)OP(A1)→ACC] 访存 2 次）；二地址（[T#B#(A1)OP(A2)→A1] 访存 4 次，用 ACC 暂存则 3 次）；三地址（[T#B#(A1)OP(A2)→A3] 访存 4 次）；四地址（含下一条指令地址 A4）。
---
解析：零地址运算类指令——操作数隐含从[T#B#栈顶和次栈顶]弹出，结果压回堆栈。
