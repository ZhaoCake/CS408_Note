[P#H1#对比]
段式虚拟存储器——虚拟地址 = [T#1#段号 + 段内地址]；段表记录段号、装入位、[T#B#段起点和段长]；物理地址 = [T#B#段首址 + 段内地址]；段长度可变，[T#!e53935#分配不便、容易产生外部碎片]。
段页式——程序按逻辑分段、每段再分固定大小的页，调入调出以[T#2#页]为基本单位；[T#!!fff59d#段长必须是页长的整数倍，段首址必须是某页的页首址]；虚地址 = [T#3#段号 + 段内页号 + 页内地址]。
---
解析：段页式寻址——按段号查段表取页表起始地址 → 与段内页号合成页表地址 → 取出实页地址 → 与页内地址拼接。
