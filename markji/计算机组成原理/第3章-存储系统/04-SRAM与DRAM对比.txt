[P#H1#对比]
SRAM——用[T#1#双稳态触发器六晶体管]记忆信息，[T#B#非破坏性读出]（无需再生）；存取速度快但集成度低、功耗大、价格贵，用于[T#B#高速缓冲存储器 Cache]。
DRAM——用[T#2#栅极电容上的电荷]存储信息（基本存储元只用一个晶体管），密度高、易集成、位价低、容量大、功耗低，但速度较慢，用于[T#B#大容量主存]。
---
解析：[T#!!fff59d#DRAM 采用地址引脚复用技术，将地址拆分为行列地址（行、列地址等长），地址引脚数减少一半（数据引脚不减少）]——SRAM 需 2^n 条地址线，DRAM 需 2^(n/2)+1 条。
