[P#H1#概念]
计算机系统层次结构：高三层属于[T#1#软件层]——高级语言层、汇编语言层、[T#B#操作系统机器层]（混合层，向上提供"广义指令"即系统调用）；低两层属于[T#2#硬件层]——传统机器层（ISA 指令集架构层）、[T#B#微指令层]（只有微程序设计的计算机系统才有）。
---
解析：高级语言层——必须用编译程序（编译器/解释器）翻译成汇编语言程序；汇编语言层——必须用汇编程序翻译成机器语言，与机器语言一一对应。
