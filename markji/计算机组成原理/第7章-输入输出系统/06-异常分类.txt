[P#H1#对比]
异常按发生原因和返回方式分三类：[T#1#故障 Fault]（[T#B#断点是当前故障指令，如缺段/缺页处理后回到故障指令继续执行]；非法操作码、除数为 0 必须终止进程）、[T#2#自陷 Trap]（[T#B#预先安排的异常，返回到自陷指令的下一条指令]，如系统调用、断点设置、单步跟踪）、[T#3#终止 Abort]（硬件故障如存储器校验错，只能终止并调出服务程序重启系统，随机发生）。
---
解析：硬故障中断（存储器校验错、总线错误）与程序性异常（整除 0、溢出、非法指令、地址越界、缺页）是异常的两大类；自陷指令若为转移指令，则返回转移目标指令。
