[P#H1#公式]
中断/查询方式防数据丢失判据：[T#!e53935#准备数据的时间（缓冲区大小 ÷ I/O 速率）必须 ≥ 取走数据的时间，否则数据被刷新丢失]；每秒查询次数至少 = [T#1#1 ÷ 取走数据的时间] = [T#2#I/O 速率 ÷ 缓冲区大小]。
取走数据时间：程序查询为[T#3#每轮查询时间]，中断方式为[T#4#中断响应 + 中断处理时间]，DMA 方式[T#B#无需 CPU 取数]。
CPU 查询时间占比计算：先算[T#5#一次数据传送所需时钟周期]，再算[T#6#每秒总传送次数（每秒中断次数）]，相乘得中断开销时钟周期，除以[T#7#主频]即为占用比例。
