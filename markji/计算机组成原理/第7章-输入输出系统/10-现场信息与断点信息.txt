[P#H1#易错]
现场与断点信息（都不能被中断服务程序破坏）：
断点信息——[T#1#PC 的值]，由[T#B#中断隐指令]（[T#B#硬件]）自动保存到栈或专门寄存器。
现场信息——[T#2#CPU 主要寄存器内容]（ACC、通用寄存器、PSW 等），对汇编程序员[T#B#可见]、可用指令直接访问，因此在[T#B#中断服务程序中用指令（软件）保存到栈]。
---
解析：为支持异常/中断嵌套，断点信息通常保存在栈中。
