[P#H1#流程]
中断响应过程（中断隐指令，[T#1#硬件]自动完成，此阶段[T#2#关中断]）：①[T#3#关中断]；②[T#4#保存断点（PC）和程序状态字 PSW]；③[T#5#识别中断源并引出中断服务程序]。
中断处理过程（中断服务程序，[T#B#软件]完成）：[T#6#保存现场和中断屏蔽字]→[T#7#开中断]（实现嵌套）→[T#8#中断事件处理]→[T#9#关中断]（保护恢复现场）→[T#10#恢复现场和屏蔽字]→[T#11#开中断，中断返回]。
---
解析：中断隐指令不是真实指令，本质是硬件自动操作，没有操作码；关中断通过中断允许触发器 IF 实现（IF=1 开中断）。
