[P#H1#易错]
[T#!!fff59d#缺页中断不是中断，是异常]（默念三遍）。
异常（内中断/例外/陷入）——信号来自[T#1#CPU 内部]、与当前执行指令有关，[T#B#必须立刻处理、无法被屏蔽]、由[T#B#CPU 自身检测]。
中断（外中断）——来自[T#2#CPU 外部设备]的中断请求，与当前执行指令无关，[T#!!fff59d#CPU 每执行完一条指令就检查中断请求信号线]。
---
解析：中断时 CPU 必须通过总线（中断请求线）获取中断源标记信息才能知道中断类型。
