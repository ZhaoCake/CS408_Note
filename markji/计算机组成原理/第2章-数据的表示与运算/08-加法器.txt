[P#H1#对比]
加法器三种结构：[T#1#一位全加器]（S = A⊕B⊕Cin，Cout = AB + (A⊕B)·Cin）；[T#2#串行进位加法器]（进位[T#B#逐位依次计算]，速度慢，一位错误可能导致全部错误）；[T#3#并行进位加法器]（先行进位/同时进位，各级进位信号[T#B#与位数无关同时形成]，但位数多时逻辑表达式过长、电路复杂，全先行进位不现实）。
---
解析：一位半加器没有进位输入，只能计算一位二进制加法；S = A⊕B，Cout = A×B。
