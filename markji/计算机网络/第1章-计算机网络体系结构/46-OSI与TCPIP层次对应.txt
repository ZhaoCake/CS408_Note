[P#H1#概念]
TCP/IP 参考模型的四层与 OSI 参考模型如何对应？各层对应哪些协议？
---
网络接口层 ↔ OSI 的[T#B#物理层 + 数据链路层]；网际层 ↔ 网络层；传输层 ↔ 传输层；应用层 ↔ OSI 的[T#B#会话层 + 表示层 + 应用层]。
对应协议：应用层 [F#1#HTTP/FTP/DNS]；传输层 TCP/UDP；网际层 [F#2#IP]；网络接口层 Ethernet/ATM/帧中继。
