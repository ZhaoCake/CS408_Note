[P#H1#概念]
TCP/IP 的网络接口层对应 OSI 的[F#1#物理层+数据链路层]；应用层对应 OSI 的[F#2#会话层+表示层+应用层]。
---
解析：完整对应——网络接口层 ↔ 物理层、数据链路层；网际层 ↔ 网络层；传输层 ↔ 传输层；应用层 ↔ 会话层、表示层、应用层。
对应协议：应用层 HTTP/FTP/DNS；传输层 TCP/UDP；网际层 IP；网络接口层 Ethernet/ATM/帧中继。
