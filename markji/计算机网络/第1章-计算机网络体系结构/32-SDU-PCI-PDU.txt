[P#H1#公式]
SDU、PCI、PDU 分别是什么？三者之间的关系式是什么？
---
[T#B#SDU 服务数据单元]：要传输的目的数据——这层[F#1#收到的东西]。
[T#B#PCI 协议控制信息]：控制协议操作的信息，即协议控制头——这层[F#2#要加的东西]。
[T#B#PDU 协议数据单元]：对等层次之间传送的数据单位——这层[F#3#要传的东西]。
关系：
[E##SDU_n + PCI_n = PDU_n = SDU_(n-1)]
即 n+1 层的 PDU 作为第 n 层的 SDU，加上本层 PCI 后变成本层 PDU 下传。
