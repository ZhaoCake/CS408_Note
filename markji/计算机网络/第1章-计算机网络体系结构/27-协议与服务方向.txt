[P#H1#对比]
协议与服务有什么区别？
---
[T#B#协议]是[F#1#水平]方向的：控制[T#B#对等实体]之间通信的规则。
[T#B#服务]是[F#2#垂直]方向的：由下层通过[T#B#层间接口]向上层提供的功能调用。
上层只能通过相邻层间的接口使用下一层的服务，不能调用其他层的服务；下层服务的实现细节对上层[T#B#透明]。
两台主机通信时，对等层在逻辑上有一条直接信道，表现为不经过下层就把信息传送到对方。
