[P#H1#对比]
HTTP 连接方式：非持久连接——每个网页元素对象都要[F#1#单独建立]一个 TCP 连接；非流水线持久连接——收到前一个响应后才能发下一个请求；[T#!!fff59d#流水线持久连接——每遇到一个对象引用就立即发出请求，HTTP/1.1 的默认方式]。
---
解析：非持久连接的传输时间 = 文档传输时间 + [F#2#2RTT]（一个 RTT 用于 TCP 连接，一个 RTT 用于请求和接收文档）。
流水线方式下默认 TCP 连接的第三个报文携带 HTTP 请求报文，之后按慢开始算法传输。
