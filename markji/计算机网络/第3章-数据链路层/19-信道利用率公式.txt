[P#H1#公式]
信道利用率 U = [F#1#TD ÷ (TD + RTT + TA)]（TD 有效发送数据时间，TA 确认帧传输时间）；忽略 TA 时 U = [F#2#(L/C) ÷ T]，T = L/C + RTT。
---
解析：信道吞吐率 = 信道利用率 × 发送方的发送速率。
求最高利用率：先算发送一帧到收到确认的总时间 TD+RTT+TA，再算这段时间能发多少帧，取以 2 为底的对数；数据帧长度不确定时应以[F#3#最短的帧长]计算。
